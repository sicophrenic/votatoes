%h1 Search

- if @movies_or_tvs.count > 0
  %table.table.table-bordered.table-hover
    %thead
      %tr
        %th.col-lg-2
          Name
        %th.col-lg-9
          Description
        %th.col-lg-1
          Add
    %tbody
      - @movies_or_tvs.each do |tvdbobj|
        %tr
          %td.col-lg-2
            %a{ :href => tvdbobj.link_to_tvdb }
              #{tvdbobj.name}
          %td.col-lg-9
            #{tvdbobj.description}
          %td.col-lg-1
            = link_to new_votato_path(tvdbobj.id), :method => :get, :class => 'btn btn-info btn-small' do
              %span.glyphicon.glyphicon-plus
- else
  Crap.