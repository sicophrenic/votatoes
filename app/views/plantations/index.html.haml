%h1 Your plantations

- if @plantations.count > 0
  %table.table.table-bordered.table-hover
    %thead
      %tr
        %th
          Name
        %th
          Count
        %th
          Most Recent Activity
        %th

    %tbody
      - @plantations.each do |plantation|
        %tr
          %td= link_to plantation.name, plantation_path(plantation)
          %td #{plantation.votatos.count}
          %td #{plantation.last_updated_at}
          %td= link_to 'Destroy', plantation, :method => :delete, :data => { :confirm => 'Are you sure?' }
  %br
- else
  .jumbotron
    .container
      %h2 You don't have any plantations!
      %p Start by creating a plantation

= link_to 'New Plantation', new_plantation_path
